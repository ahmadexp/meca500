Plan the socket class and see if the robot actually works. Since SendMessage requires a SocketInterface, we need a class that inherits from this that uses a libsocket. Probably just a regular wrapper around the libsocket class would do.
